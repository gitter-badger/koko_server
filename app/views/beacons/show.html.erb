<p id="notice"><%= notice %></p>

<p>
  <strong>Uuid:</strong>
  <%= @beacon.uuid %>
</p>

<p>
  <strong>Board:</strong>
  <% if @beacon.board.nil? %>
    -
  <% else %>
    <%= link_to @beacon.board.title, @beacon.board %>
  <% end %>
</p>

<%= link_to 'Edit', edit_beacon_path(@beacon) %> |
<%= link_to 'Beacons', beacons_path %> |
<%= link_to 'Top', boards_path %>
